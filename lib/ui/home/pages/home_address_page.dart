import 'package:flutter/material.dart';

class HomeAddressPage extends StatelessWidget {
  const HomeAddressPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Home Address Page'),
      ),
    );
  }
}
